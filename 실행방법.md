# ForgetLess - 실행 방법 가이드

## ✅ 완료된 작업
- ✅ Flutter 프로젝트 구조 생성
- ✅ 사운드 파일 7개 설정 완료 (sound_01.mp3 ~ sound_07.mp3)
- ✅ Android res/raw/ 폴더에 사운드 파일 복사
- ✅ iOS Runner/ 폴더에 사운드 파일 복사
- ✅ Flutter 패키지 설치 완료 (`flutter pub get`)

## 🚀 실행 방법

### 1. Android 에뮬레이터/실기기 실행

#### 에뮬레이터 확인
```bash
flutter devices
```

에뮬레이터가 없으면:
```bash
# Android Studio에서 AVD Manager로 에뮬레이터 생성
# 또는 실기기 연결 (USB 디버깅 활성화 필요)
```

#### 앱 실행
```bash
flutter run
```

또는 특정 기기 지정:
```bash
flutter run -d <device-id>
```

### 2. iOS 실기기/시뮬레이터 실행 (Mac 필요)

#### Xcode에서 사운드 파일 추가 (한 번만)
1. Xcode에서 `ios/Runner.xcworkspace` 열기
2. Runner 폴더 우클릭 → "Add Files to Runner..."
3. `sound_01.mp3` ~ `sound_07.mp3` 선택
4. ✅ "Copy items if needed" 체크
5. ✅ "Add to targets: Runner" 선택
6. Add 클릭

#### 앱 실행
```bash
flutter run -d ios
```

## 📱 주요 기능 테스트

### 1. 알림 설정 확인
1. 앱 실행 → 우상단 ⚙️ 설정 버튼
2. 알림 모드 선택 (멜로디/진동/무음)
3. 멜로디 선택 (7개 중 선택)
4. ▶️ 미리듣기 버튼으로 사운드 테스트

### 2. 할 일 추가
1. 홈 화면 → "전체 항목" 버튼
2. 우하단 ➕ 버튼
3. 제목 입력
4. 📅 날짜 선택 (달력)
5. 🕐 시간 선택
6. 저장

### 3. 알림 테스트
1. 가까운 시간으로 할 일 설정 (예: 1분 후)
2. 설정한 시간에 알림 발생 확인
3. **멜로디 모드**: 선택한 사운드 재생 확인 🎵
4. **진동 모드**: 진동만 확인 📳
5. **무음 모드**: 조용히 알림만 확인 🔕

## ⚙️ 설정 모드별 동작

| 모드 | 사운드 | 진동 | 설명 |
|------|--------|------|------|
| 🎵 멜로디 | ✅ | ✅ | 선택한 MP3 재생 + 진동 |
| 📳 진동 | ❌ | ✅ | 진동만 (긴 패턴) |
| 🔕 무음 | ❌ | ❌ | 알림만 조용히 표시 |

## 🎵 사운드 파일 목록

| 번호 | 파일명 | 이름 | 이모지 |
|------|--------|------|--------|
| 1 | sound_01.mp3 | 차임 | 🔔 |
| 2 | sound_02.mp3 | 딩동 | 🎵 (기본값) |
| 3 | sound_03.mp3 | 비프 | 📢 |
| 4 | sound_04.mp3 | 종소리 | 🛎️ |
| 5 | sound_05.mp3 | 트럼펫 | 🎺 |
| 6 | sound_06.mp3 | 마림바 | 🎹 |
| 7 | sound_07.mp3 | 기타 | 🎸 |

## 🔧 문제 해결

### 알림이 울리지 않을 때

#### Android
1. **권한 확인**
   - 설정 → 앱 → ForgetLess → 알림 허용
   - 설정 → 앱 → ForgetLess → 배터리 → 최적화하지 않음

2. **시스템 설정**
   - 방해 금지 모드 해제
   - 볼륨 확인

#### iOS
1. **권한 확인**
   - 설정 → ForgetLess → 알림 허용
   - 알림 스타일: 배너 또는 알림

2. **사운드 파일**
   - Xcode에서 sound_*.mp3 파일이 Runner 타겟에 추가되었는지 확인

### 사운드가 재생되지 않을 때

1. **Android**
   ```bash
   # res/raw/ 폴더 확인
   ls android/app/src/main/res/raw/
   
   # 파일이 없으면 다시 복사
   setup_sounds.bat
   ```

2. **iOS**
   - Xcode에서 Runner 프로젝트 열기
   - Project Navigator에서 sound_*.mp3 파일 확인
   - Build Phases → Copy Bundle Resources 확인

### 빌드 에러

```bash
# 클린 빌드
flutter clean
flutter pub get
flutter run
```

## 📊 프로젝트 구조

```
lib/
├── main.dart                       # 앱 진입점
├── models/task.dart                # Task 데이터 모델
├── services/
│   ├── notification_service.dart   # 알림 서비스 ⭐
│   ├── storage_service.dart        # Hive 저장소
│   └── audio_service.dart          # 오디오 재생
├── screens/
│   ├── home_screen.dart            # 홈 (오늘 할 일)
│   ├── task_list_screen.dart       # 전체 항목
│   ├── task_edit_screen.dart       # 추가/편집
│   └── settings_screen.dart        # 알림 설정 ⭐
└── ...

sound/                              # 원본 사운드 파일
android/app/src/main/res/raw/       # Android 사운드
ios/Runner/                         # iOS 사운드
```

## 🎯 다음 단계

### 개발 중
```bash
flutter run --debug
```

### APK 빌드 (Android)
```bash
flutter build apk --release
# 결과: build/app/outputs/flutter-apk/app-release.apk
```

### 실기기 설치
```bash
flutter install
```

## 📝 추가 명령어

```bash
# 연결된 기기 확인
flutter devices

# 로그 확인
flutter logs

# 핫 리로드 (앱 실행 중 'r' 키)
# 핫 리스타트 (앱 실행 중 'R' 키)

# 패키지 업데이트 확인
flutter pub outdated
```

## 🎉 완료!

이제 ForgetLess 앱이 실행 가능합니다!

- ✅ 실제 사운드/진동 알림
- ✅ 7가지 멜로디 선택
- ✅ 미리듣기 기능
- ✅ 날짜+시간 선택
- ✅ Android/iOS 지원
- ✅ 설정 저장/복원

**실행 명령:**
```bash
flutter run
```

즐거운 코딩 되세요! 🚀

